{
  "name": "python-codespace-extensions-only",
  "postCreateCommand": "sudo rm -f /etc/apt/sources.list.d/yarn.list && sudo apt update && sudo apt install -y graphviz && pip install --upgrade pip && pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  }
}
